{
    "configurations": [
        {
            "name": "Debug current file",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${file}",
            "envFile": "${workspaceFolder}/lynx-mcp-server/.env",
        }
    ]
}