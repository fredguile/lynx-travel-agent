drop table if exists emails_dev;

create table emails_dev (
  id bigint generated by default as identity not null,
  created_at timestamp with time zone not null default now(),
  content text null,
  embedding vector(1536) not null,
  metadata jsonb null,
  constraint emails_dev_pkey primary key (id)
) TABLESPACE pg_default;

comment on table emails_dev IS 'Pan PAC emails knowledge DEV (semantic search)';

create index emails_dev_embedding_idx on emails_dev using hnsw (embedding vector_cosine_ops);

drop table if exists emails_prod;

create table emails_prod (
  id bigint generated by default as identity not null,
  created_at timestamp with time zone not null default now(),
  content text null,
  embedding vector(1536) not null,
  metadata jsonb null,
  constraint emails_prod_pkey primary key (id)
) TABLESPACE pg_default;

comment on table emails_prod IS 'Pan PAC emails knowledge PROD (semantic search)';

create index emails_prod_embedding_idx on emails_prod using hnsw (embedding vector_cosine_ops);
